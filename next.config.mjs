/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',  // Ensures that next export works
  basePath: '/kingkiro99.github.io', // Set the base path to match your GitHub repo name
  assetPrefix: '/kingkiro99.github.io', // Set asset prefix for static files
};

export default nextConfig;
